{
  "name": "ic-fuzzer",
  "version": "0.1.0",
  "description": "Discover the minimal input shapes that push V8 inline caches to megamorphic",
  "keywords": ["v8", "jit", "inline-cache", "megamorphic", "deopt", "fuzzing", "performance"],
  "license": "MIT",
  "bin": {
    "ic-fuzzer": "./bin/ic-fuzzer.js"
  },
  "main": "./src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test 'test/**/*.test.js'",
    "test:watch": "node --test --watch 'test/**/*.test.js'"
  },
  "dependencies": {
    "fast-check": "^4.8.0",
    "v8-deopt-parser": "^0.4.3"
  }
}
